name: YAML error                         # ワークフロー名
on: push                            # イベント（プッシュ時に軌道）
jobs:                               # ジョブの定義
  hello:                            # ジョブID
      runs-on: ubuntu-latest        # ランナー(Ubuntuで実行)
        steps:                        # ステップの定義
        - run: echo "YAML Syntax Error"  # シェルコマンドの実行